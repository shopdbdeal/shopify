<!-- Dans votre section product-promo-bar.liquid -->
<div class="product-promo-bar">
  <div class="container">
    <div class="product-promo-bar__inner">
      <!-- Flèche précédente -->
      <button class="custom-prev-arrow">‹</button>

      <!-- Contenu principal -->
      <div class="promo-content-wrapper">
        <a href="/collections/health-beauty" class="product-promo-bar__item">
          <span class="product-promo-bar__content">
            <span class="promo-text-main">Buy 1 Get 1 25% OFF</span>
            <span class="promo-text-category">Health & Beauty</span>
          </span>
          <span class="product-promo-bar__timer">
            <span>05h : </span>
            <span>53m : </span>
            <span class="bold-seconds">25s</span>
          </span>
        </a>
      </div>

      <!-- Flèche suivante -->
      <button class="custom-next-arrow">›</button>
    </div>
  </div>
</div>

<style>
.product-promo-bar {
  background: #1a237e;
  padding: 12px 0;
  font-size: 14px;
}

.product-promo-bar__inner {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 25px;
}

.promo-content-wrapper {
  flex-grow: 1;
  max-width: 80%;
  text-align: center;
}

.product-promo-bar__item {
  color: #ffffff;
  display: inline-flex;
  align-items: center;
  gap: 15px;
  text-decoration: none;
}

.promo-text-main {
  font-weight: 500;
  margin-right: 10px;
}

.promo-text-category {
  opacity: 0.9;
}

.product-promo-bar__timer {
  display: inline-flex;
  gap: 3px;
  margin-left: 15px;
}

.bold-seconds {
  font-weight: 700;
}

/* Style des flèches */
.custom-prev-arrow,
.custom-next-arrow {
  background: none;
  border: none;
  color: white;
  font-size: 24px;
  cursor: pointer;
  padding: 0 10px;
  transition: opacity 0.3s;
}

.custom-prev-arrow:hover,
.custom-next-arrow:hover {
  opacity: 0.8;
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Initialisation Flickity avec paramètres modifiés
  new Flickity('.product-promo-bar__inner', {
    autoPlay: false,
    pageDots: false,
    prevNextButtons: false, // On utilise nos propres flèches
    wrapAround: true,
    cellAlign: 'center'
  });

  // Gestion des clics sur les flèches personnalisées
  document.querySelector('.custom-prev-arrow').addEventListener('click', () => {
    flkty.previous();
  });
  
  document.querySelector('.custom-next-arrow').addEventListener('click', () => {
    flkty.next();
  });
});
</script>